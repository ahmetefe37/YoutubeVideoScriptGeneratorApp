

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h3 class="mb-0">Generate Your Script</h3>
            </div>
            <div class="card-body">
                <form id="scriptForm">
                    <div class="mb-4">
                        <label for="subject" class="form-label">What would you like your video to be about?</label>
                        <input type="text" class="form-control form-control-lg" id="subject" name="subject" 
                               placeholder="e.g., The impact of the Silk Road on ancient trade" required>
                        <div class="form-text">Be specific for better results. Include time period, location, or key elements.</div>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg" id="generateBtn">
                            <span class="spinner-border spinner-border-sm d-none me-2" id="loadingSpinner" role="status"></span>
                            Generate Script
                        </button>
                    </div>
                </form>
            </div>
        </div>
        
        <div class="card mt-4 shadow d-none" id="resultCard">
            <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
                <h3 class="mb-0">Your Generated Script</h3>
                <button class="btn btn-sm btn-light" id="copyBtn" title="Copy to clipboard">
                    <i class="fas fa-copy"></i>
                </button>
            </div>
            <div class="card-body">
                <div id="scriptResult">
                    <!-- Generated script will appear here -->
                </div>
                <div class="d-grid gap-2 mt-4">
                    <button class="btn btn-outline-primary" id="downloadBtn">
                        <i class="fas fa-download me-2"></i>Download Script
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

